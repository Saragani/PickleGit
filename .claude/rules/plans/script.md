# Plan: script

No SC commands needed. Invoke `pkl:plan` directly to write the plan file.

No `## Tests` or `## After Implementation` section needed for pure scripts (no compiled output to analyze or test on device).
