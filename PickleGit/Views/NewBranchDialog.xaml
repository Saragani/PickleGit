<Window x:Class="PickleGit.Views.NewBranchDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create Branch" Height="240" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False"
        Icon="pack://application:,,,/Resources/pickle.ico"
        FocusManager.FocusedElement="{Binding ElementName=NameBox}"
        Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Create New Branch" FontSize="15" FontWeight="SemiBold"/>

        <TextBlock Grid.Row="2" Text="Branch name" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
        <TextBox Grid.Row="4" Name="NameBox" Style="{StaticResource DarkTextBox}"
                 Text="{Binding BranchName, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Grid.Row="5" x:Name="ValidationText" FontSize="11" TextWrapping="Wrap"
                   Margin="0,8,0,0" VerticalAlignment="Top"
                   Foreground="{DynamicResource DangerBrush}" Visibility="Collapsed"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource ToolbarButton}" Margin="0,0,8,0"
                    Width="80" Click="Cancel_Click" IsCancel="True"/>
            <Button Content="Create" Style="{StaticResource AccentButton}" Width="80"
                    Click="Create_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
