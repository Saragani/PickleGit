<Window x:Class="PickleGit.Views.Dialogs.TextPromptDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding DialogTitle}" MinHeight="200" Width="440"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        Icon="pack://application:,,,/Resources/pickle.ico"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../../Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding HeaderText}" FontSize="15" FontWeight="SemiBold"/>

        <TextBlock Grid.Row="2" Text="{Binding PromptText}" FontSize="12"
                   Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap"/>
        <TextBox Grid.Row="4" Name="InputBox" Style="{StaticResource DarkTextBox}"
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"/>

        <CheckBox Grid.Row="5" Content="{Binding CheckboxText}"
                  IsChecked="{Binding IsCheckboxChecked}"
                  Visibility="{Binding CheckboxVisibility}"
                  VerticalAlignment="Top" Margin="0,10,0,0"
                  Foreground="{DynamicResource TextPrimaryBrush}" FontSize="12"/>

        <StackPanel Grid.Row="6" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Style="{StaticResource ToolbarButton}" Margin="0,0,8,0"
                    MinWidth="80" Click="Cancel_Click" IsCancel="True"/>
            <Button Content="{Binding OkText}" Style="{StaticResource AccentButton}" MinWidth="80"
                    Click="Ok_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
