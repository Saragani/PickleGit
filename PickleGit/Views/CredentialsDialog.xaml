<Window x:Class="PickleGit.Views.CredentialsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Authentication Required" Height="280" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" ShowInTaskbar="False"
        Icon="pack://application:,,,/Resources/pickle.ico"
        FocusManager.FocusedElement="{Binding ElementName=UsernameBox}"
        Background="{DynamicResource BackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Enter your credentials" FontSize="15" FontWeight="SemiBold"/>

        <TextBlock Grid.Row="2" Text="Username" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
        <TextBox Grid.Row="4" x:Name="UsernameBox" Style="{StaticResource DarkTextBox}"/>

        <TextBlock Grid.Row="6" Text="Password / App Password" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}"/>
        <PasswordBox Grid.Row="8" x:Name="PassBox"
                     Style="{StaticResource DarkPasswordBox}"
                     PasswordChanged="PassBox_PasswordChanged"/>

        <TextBlock Grid.Row="9" x:Name="ValidationText" FontSize="11" TextWrapping="Wrap"
                   Margin="0,8,0,0" VerticalAlignment="Top"
                   Foreground="{DynamicResource DangerBrush}" Visibility="Collapsed"/>

        <StackPanel Grid.Row="10" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Style="{StaticResource ToolbarButton}" Margin="0,0,8,0"
                    Width="80" Click="Cancel_Click" IsCancel="True"/>
            <Button Content="OK" Style="{StaticResource AccentButton}" Width="80"
                    Click="OK_Click" IsDefault="True"/>
        </StackPanel>
    </Grid>
</Window>
